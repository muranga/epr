<p id="notice"><%= notice %></p>

<table border="0" cellspacing="5" cellpadding="5">
	<tr><th><%=@report.title %></th></tr>
	<tr><td>
		Id
	</td>
	<td>
		<%=@report.id %>
	</td>
</tr>
	<tr><td>
		Title
	</td>
	<td>
		<%=@report.title %>
	</td>
</tr><tr><td>
		Abstract
	</td>
	<td>
		<%=@report.abstract %>
	</td>
</tr>
	<tr><td>
		File
	</td>
	<td>
	    <% if @report.document? %>
	        <%= link_to @report.document.url, :class => "button small" do %>
	            <%= @report.document.file.filename %>
	            <i class="icon-download-alt"></i>
	        <% end %>
	    <% end %>
		
	</td>
</tr>
</table>

<%= link_to 'Back', reports_path %>





<%= link_to 'Edit', edit_report_path(@report) %> |
<%= link_to 'Back', reports_path %>
